import { useQuery } from "@tanstack/react-query";
import React from "react";
import useAxiosPublic from "../../hooks/useAxiosPublic";

const BestWorkers = () => {
//   const { data } = useQuery({
//     queryKey: ["best-workers"],
//     queryFn: async () => {
//       const { data } = await useAxiosPublic.get("users");
//       return data;
//     },
//   });
//   console.log(data);
  return <div></div>;
};

export default BestWorkers;
